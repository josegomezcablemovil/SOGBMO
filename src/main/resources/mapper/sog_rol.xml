<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.sog_rol">
    <select id="selectRol" parameterType="org.sog.persistence.entities.EBRol"
            resultType="org.sog.persistence.entities.EBRol">
        SELECT * FROM sog_rol;
    </select>
</mapper>